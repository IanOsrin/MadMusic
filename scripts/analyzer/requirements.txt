# Essentia ships manylinux wheels for CPython 3.8–3.11 — pin Render's runtime to
# Python 3.11 (PYTHON_VERSION=3.11.x) so pip installs a prebuilt wheel rather
# than trying to compile. Left unpinned to get the matching wheel for that runtime.
essentia
boto3>=1.34
requests>=2.31
numpy>=1.26,<2
